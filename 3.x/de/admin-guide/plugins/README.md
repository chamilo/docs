# Plugins

Plugins erweitern die Funktionalität von Chamilo, ohne den Kerncode zu verändern. Chamilo 3.0 wird mit über 50 Plugins ausgeliefert, die bei Bedarf aktiviert werden können.

## Der Block Plugins

Das Administrations-Dashboard verfügt außerdem über einen eigenen Block **Plugins**, der von der allgemeinen Plugin-Verwaltung in diesem Kapitel getrennt ist. Er listet nicht jedes installierte Plugin auf — nur diejenigen, deren Konfiguration festlegt, dass sie in der Region des Administrationsmenüs erscheinen sollen, und gibt jedem solchen Plugin eine direkte Verknüpfung zu seiner eigenen Administrationsseite direkt auf dem Dashboard.

![Der Block Plugins auf dem Administrations-Dashboard mit Verknüpfungen zu installierten Plugins, die so konfiguriert sind, dass sie im Administrationsmenü erscheinen](/.gitbook/assets/admin-plugins-block.png)

Wenn ein von Ihnen aktiviertes Plugin hier nicht erscheint, ist das zu erwarten — das bedeutet, dass dieses Plugin sich nicht für die Region des Administrationsmenüs deklariert hat, nicht dass etwas defekt ist. Siehe [Plugins verwalten](managing-plugins.md) für die vollständige Liste der installierten Plugins, unabhängig davon, ob sie auf dem Dashboard erscheinen.

## In diesem Kapitel

* **[Plugins verwalten](managing-plugins.md)** — Plugins installieren, aktivieren, konfigurieren und deaktivieren