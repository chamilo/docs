# Multi-URL

Chamilo's multi-URL feature allows you to run multiple virtual portals from a single Chamilo installation. Each portal has its own URL, branding, users, courses, and settings, but they all share the same database and codebase.

* **[Access URLs](access-urls.md)** — Configure and manage multiple access URLs
